// =======================================================================================
// Pure (纯净对抗) Confogl Plugins
// =======================================================================================
// Pure: universal plugins are loaded globally via cfg/generalfixes.cfg (server.cfg match chain).
// No competitive fix pack (cfg/compfixes.cfg).

exec cfgogl/pure/shared_plugins.cfg
