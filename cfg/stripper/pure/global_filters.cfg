// Pure (纯净对抗) uses NO map edits - all maps run vanilla (no stripper configs).
// This folder intentionally only contains this commented file, matching the
// stripper_cfg_path set in cfg/cfgogl/pure/shared_cvars.cfg.
