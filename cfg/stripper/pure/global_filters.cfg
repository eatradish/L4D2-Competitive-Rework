// Pure (纯净对抗) keeps maps vanilla - no global filters and no gameplay map
// edits. The ONLY exception is maps/c2m3_coaster.cfg, a backport of the zonemod
// fix so the c2m3 coaster cannot kill Special Infected.
// This folder is used via the stripper_cfg_path set in cfg/cfgogl/pure/shared_cvars.cfg.
